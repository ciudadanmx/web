// index.js
import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; // Asegúrate de importar App.js

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App /> {/* Este es el punto de entrada de tu aplicación */}
  </React.StrictMode>
);
